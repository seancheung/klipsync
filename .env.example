# KlipSync 环境变量示例
#
# 使用方式：
#   本地开发 —— 复制为 .env.local，按需修改
#   生产部署 —— 通过 docker-compose.yml 的 environment 注入（不要打进镜像）
#
# 详细解释见 .faststack/technical.md §11.1。

# ──────────────────────────────────────────────────────────
# 必填（建议显式设置）
# ──────────────────────────────────────────────────────────

# 数据根目录：SQLite 文件 + 附件都落在此目录
#   - 本地开发用相对路径 ./data
#   - 容器内固定为 /data（由卷挂载映射到宿主）
DATA_DIR=./data

# ──────────────────────────────────────────────────────────
# 可选
# ──────────────────────────────────────────────────────────

# Drizzle / better-sqlite3 连接串；默认由 DATA_DIR 推导，通常不需要改
#DATABASE_URL=file:./data/klipsync.db

# 运行环境
#   - development：开启更严格的错误提示、禁用 Service Worker 缓存
#   - production：Docker 默认；关闭源码映射
NODE_ENV=development

# Next.js 监听端口；容器内固定 3000
PORT=3000

# 反向代理前置：开启后从 X-Forwarded-For 取真实 IP 用于登录限流
#   - 本地开发：false
#   - 生产（容器背后 Caddy / NPM / Traefik）：true
#   反代必须透传 X-Forwarded-For / X-Forwarded-Proto，否则限流会被反代 IP 打穿
TRUST_PROXY=false

# Cookie Secure 标志
#   - 本地 HTTP dev：false
#   - 生产（反代终止 TLS）：true（浏览器只会在 HTTPS 下发送该 cookie）
COOKIE_SECURE=false

# 日志等级：debug | info | warn | error，默认 info
#   - 结构化 JSON 行输出到 stdout / stderr，由 Docker 默认捕获
#LOG_LEVEL=info
